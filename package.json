{
  "devDependencies": {
    "eslint": "^7.32.0",
    "jest": "^27.1.0",
    "jest-fetch-mock": "^3.0.3",
    "jest-puppeteer": "^5.0.4"
  },
  "scripts": {
    "test": "jest --watchAll --detectOpenHandles"
  },
  "jest": {
    "preset": "jest-puppeteer"
  },
  "dependencies": {
    "puppeteer": "^10.2.0"
  }
}
